# tests

This folder contains automated tests mirroring the `src/` project structure.

## Rules
- Match naming with production project and test type.
- Keep tests deterministic and isolated.
- Prioritize behavior-focused assertions.
